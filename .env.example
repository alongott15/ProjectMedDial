# Azure AI Configuration
# Get these from your Azure AI deployment
AZURE_AI_ENDPOINT=https://your-endpoint.inference.ai.azure.com
AZURE_AI_API_KEY=your_api_key_here

# MIMIC-III Database Configuration (optional - only if using SQL instead of CSV)
DATABASE_URL=postgresql://username:password@host:port/mimic

# MIMIC-III CSV Directory (optional - only if using CSV instead of SQL)
MIMIC_CSV_DIR=/path/to/mimic-iii/csv

# Example:
# MIMIC_CSV_DIR=/home/user/data/mimic-iii-csv
# DATABASE_URL=postgresql://mimic_user:password@localhost:5432/mimic
