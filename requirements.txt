transformers>=4.30.0
torch>=2.0.0
pydantic>=1.10.2
azure-ai-inference  # For Azure AI Foundry / GPT-4.1 inference
python-dotenv
pandas>=1.5.0  # For CSV data loading

# --- Evaluation framework ---
deepeval>=0.21.0          # GEval metrics and DeepEvalBaseLLM adapter
azure-core>=1.30.0        # Required by azure-ai-inference
